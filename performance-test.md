# Voisk 프론트엔드 성능 테스트

**테스트 환경**
- 기기: iPhone 16 Pro Max
- 브라우저: Safari
- VoiceOver: ON (전 항목 공통)
- API 측정: Mac에 USB 연결 → Safari 개발자 메뉴 → Web Inspector 네트워크 탭
- 로딩 측정: pagespeed.web.dev 에 `www.voisk.cloud` 입력

---

## 1. 화면 로딩 성능

**측정 목적**
사용자가 `www.voisk.cloud` 에 접속했을 때 첫 화면이 얼마나 빨리 뜨는지 확인.

**측정 방법**
`pagespeed.web.dev` → URL 입력 → Mobile 기준으로 분석 실행.

**측정 항목**

| 항목 | 설명 | 측정값 |
|------|------|--------|
| FCP | 첫 글자나 버튼이 화면에 나타나는 시간 | 초 |
| LCP | 주요 화면이 거의 다 뜨는 시간 | 초 |
| TBT | 화면이 멈춘 것처럼 느껴지는 시간 | ms |
| CLS | 화면이 갑자기 밀리는 정도 |  |
| Performance Score | 종합 점수 | / 100 |

**보고서에 쓰기 좋은 문장**
- "PageSpeed Insights를 활용해 초기 화면 로딩 성능을 측정하였다."
- "측정 결과 FCP는 `[ ]`초, LCP는 `[ ]`초로 나타났다."

---

## 2. API 응답 지연 테스트

**측정 목적**
사용자가 텍스트 필드에 주문 문장을 입력한 뒤, 백엔드 응답을 받아 화면이 바뀌기까지 걸리는 시간 확인.

**측정 방법**
iPhone USB 연결 → 맥 Safari → 개발자 메뉴 → iPhone > voisk.cloud → Web Inspector 네트워크 탭 열고 아래 시나리오 수행.

**테스트 시나리오**
1. 즉시 주문에서 `"슈크림 라떼 1개 아이스로 톨 사이즈 주세요"` 입력
2. 메뉴 추천에서 `"달달한 거 추천해줘"` 입력
3. 추천 문장 버튼 선택
4. 주문 확인 화면에서 추가 옵션 선택
5. 이대로 주문 버튼 클릭

**측정 항목**

| API | 시나리오 | 응답 시간 |
|-----|----------|----------|
| `POST /api/order/speak` | 즉시 주문 문장 입력 | ms |
| `GET /api/recommend/hints` | 추천 화면 진입 | ms |
| `GET /api/recommend/hints/{hintId}` | 추천 버튼 선택 | ms |
| `GET /api/order/required-option-summary` | 필수 옵션 화면 진입 | ms |
| `GET /api/order/menus/{menuId}/optional-options` | 선택 옵션 화면 진입 | ms |
| `POST /api/order/option-selection` | 옵션 선택 완료 | ms |

"처리 중입니다" 안내 발생 기준: **800ms**
실제 발생 횟수: / 전체 요청 회

**보고서에 쓰기 좋은 문장**
- "`/api/order/speak` 평균 응답 시간: `[ ]` ms"
- "`/api/recommend/hints/{hintId}` 평균 응답 시간: `[ ]` ms"
- "처리 중 안내 발생 비율: `[ ]`%"

---

## 3. VoiceOver 포커스 이동 테스트

**측정 목적**
스크린리더 사용자가 스와이프 몇 번으로 핵심 기능에 도달하는지 확인.

**측정 방법**
VoiceOver ON 상태에서 각 화면 진입 후 목표 요소까지 오른쪽 스와이프 횟수를 직접 세어 기록.

**측정 항목**

| 화면 | 목표 요소 | 스와이프 횟수 | 문제 여부 |
|------|----------|-------------|---------|
| 첫 화면 | 메뉴판 버튼 | 회 | 없음 / 있음 |
| 첫 화면 | 메뉴 추천 버튼 | 회 | 없음 / 있음 |
| 첫 화면 | 즉시 주문 버튼 | 회 | 없음 / 있음 |
| 주문 입력 화면 | 텍스트 필드 | 회 | 없음 / 있음 |
| 필수 옵션 화면 | 온도 선택 버튼 | 회 | 없음 / 있음 |
| 주문 확인 화면 | 이대로 주문 버튼 | 회 | 없음 / 있음 |

**보고서에 쓰기 좋은 문장**
- "VoiceOver 환경에서 핵심 버튼까지 도달하는 스와이프 횟수를 측정하였다."
- "불필요한 안내 요소를 제거하여 주문 확인 화면의 탐색 부담을 줄였다."

---

## 4. 주문 완료까지 걸리는 시간

**측정 목적**
사용자가 실제로 주문을 끝내는 데 걸리는 시간을 확인.

**측정 방법**
스톱워치로 구간별 직접 측정. VoiceOver ON 상태 유지.

### 시나리오 1 — 즉시 주문
입력 문장: `"슈크림 라떼 1개 아이스로 톨 사이즈 주세요"`

| 측정 구간 | 소요 시간 |
|----------|----------|
| 첫 화면 진입 → 즉시 주문 선택 | 초 |
| 텍스트 필드 입력 → 전송 | 초 |
| 전송 → 주문 확인 화면 표시 | 초 |
| 이대로 주문 클릭 → 완료 화면 표시 | 초 |
| **전체 소요 시간** | **초** |

API 호출 횟수: 회 / 스와이프 횟수: 회 / 직접 선택한 버튼: 회

### 시나리오 2 — 메뉴 추천 주문
입력 문장: `"달달한 거 추천해줘"` → 추천 메뉴 선택 → 필수 옵션 선택 → 이대로 주문

| 측정 구간 | 소요 시간 |
|----------|----------|
| 첫 화면 진입 → 추천 입력 전송 | 초 |
| 전송 → 추천 결과 표시 | 초 |
| 메뉴 선택 → 옵션 선택 완료 | 초 |
| 이대로 주문 클릭 → 완료 화면 표시 | 초 |
| **전체 소요 시간** | **초** |

API 호출 횟수: 회 / 스와이프 횟수: 회 / 직접 선택한 버튼: 회

**보고서에 쓰기 좋은 표**

| 시나리오 | 완료 시간 | API 호출 수 | 스와이프 횟수 | 성공 여부 |
|---------|----------|------------|-------------|---------|
| 즉시 주문 | 초 | 회 | 회 | 성공 / 실패 |
| 메뉴 추천 주문 | 초 | 회 | 회 | 성공 / 실패 |

---

## 5. 안내 음성 끊김 테스트

**측정 목적**
VoiceOver 안내가 중간에 끊기거나, 다른 요소로 포커스가 튀는지 확인.

**측정 방법**
VoiceOver ON 상태에서 각 화면 진입 후 안내 음성을 끝까지 청취하며 아래 체크리스트 확인.

**체크리스트**

| 화면 | 진입 안내 끝까지 읽힘 | 포커스가 텍스트 필드로 튀지 않음 | 버튼 선택 후 과도한 반복 없음 | "처리 중" 안내 타이밍 적절 | 선택 완료 안내 한 번만 나옴 |
|------|---------------------|-------------------------------|---------------------------|------------------------|--------------------------|
| 첫 화면 | Y / N | Y / N | Y / N | Y / N | Y / N |
| 메뉴 추천 화면 | Y / N | Y / N | Y / N | Y / N | Y / N |
| 필수 옵션 화면 | Y / N | Y / N | Y / N | Y / N | Y / N |
| 주문 확인 화면 | Y / N | Y / N | Y / N | Y / N | Y / N |
| 주문 완료 화면 | Y / N | Y / N | Y / N | Y / N | Y / N |

**보고서에 쓰기 좋은 문장**
- "주문 확인 화면에서 안내 도중 포커스가 입력창으로 이동하던 문제를 발견하고, 자동 포커스 이동을 제거하여 안내 문구가 끝까지 읽히도록 개선하였다."

---

## 6. 음성 인식률 테스트

**측정 목적**
받아쓰기 기능으로 주문 문장을 입력했을 때 의도한 대로 인식되는 비율 확인.

**측정 방법**
텍스트 필드에서 iPhone 받아쓰기 버튼 활성화 후 동일 문장을 5회 반복 발화. 올바르게 인식된 횟수 기록.

**테스트 문장**

| 발화 문장 | 성공 횟수 / 5회 | 인식률 |
|----------|--------------|--------|
| `"슈크림 라떼 아이스로 주세요"` | / 5 | % |
| `"달달한 거 추천해줘"` | / 5 | % |
| `"말차 라떼 핫으로 톨 사이즈 주세요"` | / 5 | % |
| `"아이스 아메리카노 한 잔 주세요"` | / 5 | % |

인식 실패 시 주요 원인: (묵음 / 오인식 / 타임아웃)

**보고서에 쓰기 좋은 문장**
- "iPhone 받아쓰기 기능을 활용한 음성 입력 인식률을 측정한 결과, 주문 문장 기준 평균 `[ ]`%의 정확도를 보였다."

---

## 최종 요약

| 테스트 항목 | 측정 방법 | 결과 | 개선 내용 |
|-----------|---------|------|---------|
| 초기 로딩 (LCP) | PageSpeed Insights | 초 | |
| 주문 응답 (`/speak`) | Safari Web Inspector | ms | |
| 즉시 주문 완료 시간 | 스톱워치 | 초 | |
| 추천 주문 완료 시간 | 스톱워치 | 초 | |
| VoiceOver 탐색 (즉시 주문) | iPhone 실기기 | 회 스와이프 | |
| 안내 끊김 | VoiceOver | 없음 / 있음 | |
| 음성 인식률 | iPhone 받아쓰기 | % | |
